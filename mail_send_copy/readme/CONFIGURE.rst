By default, this feature is enabled for all users.
If some users doesn't want to receive the mails, they can disable the feature
on the preference form:

.. figure:: ../static/description/view_res_users_simple.png
